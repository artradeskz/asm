переименовать регистр рис в рси
переименовать _start в _начало
писать напрямую в машинных кодах?
писать документацию
нет поддержки [reg + смещение], поэтому даже простая запись двух байт — проблема.
    Преобразование чисел в строки — это отдельная, нетривиальная подпрограмма, которую нужно отлаживать независимо. 
     

Не пытайся писать 10 - 25 → "-15" за один раз.
Раздели на: 

    Вычитание → работает?
    Деление с остатком → работает?
    Запись цифр в буфер → работает?
    Добавление знака → работает?
    Вывод → работает?
     

Проверяй каждый этап. 
